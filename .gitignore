# Secrets — never commit
.env
.env.*
!.env.example
*.local
*.pem

# Dependencies (none expected — zero runtime deps)
node_modules/

# Vercel
.vercel/

# Real-mode outputs (bring-your-own; not part of the public sample)
assets/processed/

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
